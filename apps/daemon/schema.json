{
  "$ref": "#/definitions/Config",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Config": {
      "additionalProperties": false,
      "properties": {
        "dnsServers": {
          "description": "DNS servers for the session to use.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "docker": {
          "$ref": "#/definitions/DockerConfig"
        },
        "host": {
          "default": "0.0.0.0",
          "description": "Host to run stardustd on",
          "type": "string"
        },
        "port": {
          "default": 4000,
          "description": "Port to run stardustd on",
          "type": "number"
        },
        "service": {
          "default": true,
          "description": "Run stardustd as a systemd/launchd service.",
          "type": "boolean"
        },
        "session": {
          "$ref": "#/definitions/SessionConfig"
        },
        "token": {
          "description": "Token used to authorize stardust servers. Uses an auto-generated token by default. Do not change unless you know what you're doing.",
          "type": "string"
        }
      },
      "required": [
        "docker",
        "session"
      ],
      "type": "object"
    },
    "DockerConfig": {
      "additionalProperties": false,
      "properties": {
        "enableIcc": {
          "default": false,
          "description": "Enable interconnectivity between containers. Do not enable unless you know what you're doing.",
          "type": "boolean"
        },
        "host": {
          "description": "The host to connect to, if using an HTTP connection.",
          "type": "string"
        },
        "network": {
          "default": "`stardust`",
          "description": "The Docker network used for connecting to containers",
          "type": "string"
        },
        "port": {
          "description": "The port for the docker host, if using an HTTP connection.",
          "type": "number"
        },
        "protocol": {
          "description": "Protocol, if using an HTTP connection",
          "enum": [
            "http",
            "https"
          ],
          "type": "string"
        },
        "socket": {
          "default": "`/var/run/docker.sock`",
          "description": "The path to the Docker socket to connect to, if using a socket connection.",
          "type": "string"
        },
        "type": {
          "default": "`socket`",
          "description": "The type of connection to use to connect to the Docker daemon.",
          "enum": [
            "http",
            "socket"
          ],
          "type": "string"
        }
      },
      "required": [
        "network"
      ],
      "type": "object"
    },
    "SessionConfig": {
      "additionalProperties": false,
      "properties": {
        "vncPassword": {
          "description": "Default VNC password to use. Defaults to randomly generated.",
          "type": "string"
        }
      },
      "type": "object"
    }
  }
}
